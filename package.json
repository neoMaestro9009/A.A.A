{
  "name": "a-a-a",
  "version": "1.0.0",
  "description": "OmniVerse Profile with Vercel Speed Insights",
  "private": true,
  "scripts": {
    "build": "echo 'Static HTML - no build required'",
    "lint": "echo 'No linter configured for static HTML'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.1.0"
  }
}
